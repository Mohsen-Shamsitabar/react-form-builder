import { Box } from "@mui/material";
import { Body, Header } from "./components";

const Dashboard = () => {
  return (
    <Box>
      <Header />
      <Body />
    </Box>
  );
};

export default Dashboard;
